# build products
meshsorter
certify_rep
__pycache__/
# results/cache is a keyed cache of the same runs that results/raw records in
# readable form; the raw results and the LaTeX bodies are committed, the cache
# is not.
results/cache/
sweep_rep
